# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

use_frameworks!

def common_pods
  pod 'Kingfisher', '~> 4.0'
end

target 'LevelShoes' do
  common_pods
end  

target 'LevelShoesTests' do
    inherit! :search_paths
    common_pods
    pod 'SnapshotTesting', '~> 1.8.1'
 end